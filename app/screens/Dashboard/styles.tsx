import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    title: {
        fontFamily: 'NotoSans-BlackItalic',
        fontSize: 24,
        marginBottom: 20,
    },
});

export default styles;
